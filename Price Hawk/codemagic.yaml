workflows:
  pricehawk-app-build:
    name: Build PriceHawk App
    environment:
      flutter: stable
      vars:
        FIREBASE_CLI_LOGIN: encrypted:your_encrypted_firebase_token
    scripts:
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
    publishing:
      email:
        recipients:
          - prakharmishraspn91@gmail.com